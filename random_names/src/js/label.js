export default class Label {
    constructor(input_ref, name, id, value) {
        this.input_ref = input_ref
        this.name = name
        this.id = id
        this.value = value
        this.s_text = value
    }
}